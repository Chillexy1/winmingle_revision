function pointer in C  is a type of pointer that stores the address of a function, allowing functions to be passed as arguments and invoked dynamically.

In this lesson we learn't how to implement and use a function pointers.

RULES AND  REQUIREMENTS;

    All files are to be compiled on Ubuntu 20.04 LTS with:gcc -Wall -Werror -Wextra -pedantic -std=gnu89

    All files compiled on Ubuntu 20.04 LTS with:

    Only C standard library functions allowed: malloc, free, exit. (Exceptions in specific tasks where printf, etc. are explicitly allowed.)

    You allowed to use _putchar



