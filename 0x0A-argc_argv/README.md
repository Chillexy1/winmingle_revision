this project teaches on what is argc and how to use argv also how command-line arguments are passed into the main() definition.

RULES AND REQUIREMENTS:

    All C files are to be compiled on Ubuntu 20.04 LTS with the flag: gcc -Wall -Werror -Wextra -pedantic -std=gnu89

    standard libraries(printf, atoi, e.t.c) is allowed also.
