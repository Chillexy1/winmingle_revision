Topic: Pointers to pointers, multidimensional arrays, and common string helpers

this training emphasizes on pointers to pointers, arrays and common strings 

RULES:
    
    only _putchar is allowed to print out  results except stated otherwise
    
    All files compiled on Ubuntu 20.04 LTS with:

    gcc -Wall -Werror -Wextra -pedantic -std=gnu89
